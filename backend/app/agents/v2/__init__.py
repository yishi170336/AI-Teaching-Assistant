"""Versioned CircuitMind teaching-agent control plane.

Submodules are intentionally imported explicitly by callers so the V2 engine
can reuse proven domain helpers without creating a package import cycle.
"""
