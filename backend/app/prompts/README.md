# CircuitMind V2 提示词

本目录保存产品运行时的版本化 Agent 角色模板。业务代码只传入当前任务、受控输入和输出 Schema；模板中的权限顺序、上下文隔离、失败策略与禁止事项不得由用户输入覆盖。

- 所有服务型角色固定使用 `qwen3.7-flash`、temperature 0、JSON 模式和关闭 thinking。
- 内容生成角色可使用学生选择的文本模型，但仍必须遵守角色模板和质量门。
- 模板及注册信息共同生成 `prompt_bundle_version`，用于审计、缓存和回归。
- 不在提示词中要求或保存思维链，只保留可验证结论、检查项和证据标识。
